<UserControl x:Class="ProjectTemplate_v2.Views.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:telerik="http://schemas.telerik.com/2008/xaml/presentation"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="542" d:DesignWidth="1016"
             Padding="20">


    <UserControl.Resources>
        <DataTemplate x:Key="gridcho1">
            
        </DataTemplate> 

    </UserControl.Resources>
    
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation
              Storyboard.TargetProperty="Opacity"
              From="0.0" To="1.0" Duration="0:0:0.5"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>

    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="950" Height="450">
        <UniformGrid Columns="4">
            <UniformGrid.Resources>
                <Style TargetType="RadioButton" BasedOn="{StaticResource MaterialDesignTabRadioButton}">
                    <Setter Property="Padding" Value="0"/>
                </Style>
            </UniformGrid.Resources>
            <RadioButton Content="Creators" IsChecked="True"
                                     Checked="CreatorsPage_Checked"/>
            <RadioButton Content="Dashboard page"
                                     Checked="DashboardPage_Checked"/>
            <RadioButton Content="Sensors Page"
                                     Checked="SensorsPage_Checked"/>
            <RadioButton Content="Map Page"
                                     Checked="MapPage_Checked"/>
        </UniformGrid>

        <Canvas>
            <materialDesign:Card x:Name="card1" Visibility="Hidden"  Width="200" Canvas.Left="40" Canvas.Top="141">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="140" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Image
                          Source="{Binding ImagePath1}"
                          Height="140"
                          Width="196"
                          Stretch="UniformToFill" />

                    <StackPanel Grid.Row="1" Margin="8 8 8 0">
                        <TextBlock x:Name="txtBlock1Header" Margin="10" FontWeight="Bold"/>
                        <TextBlock x:Name="txtBlock1Text" TextWrapping="Wrap" Margin="10" VerticalAlignment="Center"/>
                    </StackPanel>

                </Grid>
            </materialDesign:Card>

            <materialDesign:Card x:Name="card2" Visibility="Hidden" Width="200" Canvas.Left="274" Canvas.Top="32">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="140" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Image
                          Source="{Binding ImagePath2}"
                          Height="140"
                          Width="196"
                          Stretch="UniformToFill" />

                    <StackPanel Grid.Row="1" Margin="8 8 8 0">
                        <TextBlock Text="{Binding card2Header}" Margin="10" FontWeight="Bold"/>
                        <TextBlock Text="{Binding card2Text}" TextWrapping="Wrap" Margin="10" VerticalAlignment="Center"/>
                    </StackPanel>

                </Grid>
            </materialDesign:Card>

            <materialDesign:Card x:Name="card3" Visibility="Hidden" Width="200" Canvas.Left="504" Canvas.Top="141">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="140" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Image
                          Source="{Binding ImagePath3}"
                          Height="140"
                          Width="196"
                          Stretch="UniformToFill" />

                    <StackPanel Grid.Row="1" Margin="8 8 8 0">
                        <TextBlock Text="{Binding card3Header}" Margin="10" FontWeight="Bold"/>
                        <TextBlock Text="{Binding card3Text}" TextWrapping="Wrap" Margin="10" VerticalAlignment="Center"/>
                    </StackPanel>

                </Grid>
            </materialDesign:Card>

            <materialDesign:Card x:Name="card4" Visibility="Hidden" Width="200" Canvas.Left="738" Canvas.Top="60">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="140" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Image
                          Source="{Binding ImagePath4}"
                          Height="140"
                          Width="196"
                          Stretch="UniformToFill" />

                    <StackPanel Grid.Row="1" Margin="8 8 8 0">
                        <TextBlock Text="{Binding card4Header}" Margin="10" FontWeight="Bold"/>
                        <TextBlock Text="{Binding card4Text}" TextWrapping="Wrap" Margin="10" VerticalAlignment="Center"/>
                    </StackPanel>

                </Grid>
            </materialDesign:Card>
        </Canvas>


        
    </StackPanel>
</UserControl>
